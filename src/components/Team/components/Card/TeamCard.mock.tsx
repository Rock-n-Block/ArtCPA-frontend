import { FirstCard } from 'assets/img/icons';
import { TeamCardProps } from './TeamCard';

export const cardPropsMocked: TeamCardProps = {
  image: FirstCard, personName: 'Name', personPosition: 'position',
};
